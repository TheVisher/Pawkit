node_modules/
.next/
.env
.env.local
.env.production
.env.production.local
.env.development.local
*.log
.DS_Store
dev.db
dev.db-journal
nul

# Database files
prisma/dev.db
prisma/dev.db-journal

# Extension build artifacts
packages/extension/dist-*
packages/extension/pawkit-*/assets/*
packages/extension/pawkit-chrome 2/

# Keep backups local only - NEVER commit actual database backups
backups/

# Keep environment templates in git, but not actual env files
!.env.*.template

# DO NOT ignore these critical files - they must be in git:
# .claude/instructions.md - All safety rules, project context, auto-loads every chat
# scripts/protect-production-db.js
# scripts/pre-migration-check.js
# PAWKIT_BIBLE.md - Single source of truth for project context
