# Production Environment Template
# NEVER commit actual production values to git!

# Production Database (Supabase PostgreSQL)
DATABASE_URL="postgresql://postgres:PASSWORD@db.xxx.supabase.co:5432/postgres"
DIRECT_URL="postgresql://postgres:PASSWORD@db.xxx.supabase.co:5432/postgres"

# Supabase Authentication
NEXT_PUBLIC_SUPABASE_URL="https://xxx.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="your-anon-key"

# Production Safety Flag
# Set this to 'true' ONLY when you explicitly want to run migrations on production
# NEVER set this as a permanent environment variable - only use it temporarily
ALLOW_PRODUCTION_MIGRATION=false

# Node Environment
NODE_ENV=production

# Filen Cloud Storage Encryption (REQUIRED)
# Must be exactly 32 characters for AES-256 encryption
# Generate with: openssl rand -base64 24 | head -c 32
FILEN_ENCRYPTION_KEY="generate-a-secure-32-char-key!!"
